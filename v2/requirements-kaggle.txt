# Kaggle provides the CUDA torch/transformers/accelerate stack. Do not install
# or upgrade those core packages from this file: unpinned versions can break
# imports (for example torch.utils._typing_utils vs Transformers 5.x).

# Retrieval dependencies. vLLM is checked separately in the notebook's
# inference bootstrap so training does not depend on the LLM runtime.
transformers>=4.41,<5
accelerate>=1.1.0
sentencepiece>=0.2
safetensors>=0.4
bm25s
faiss-cpu
sentence-transformers
